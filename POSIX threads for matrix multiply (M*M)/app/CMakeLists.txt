CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(Matrix C)
SET(TARGET_NAME Matrix)
AUX_SOURCE_DIRECTORY(. SRC)
ADD_EXECUTABLE(Matrix ${SRC})
TARGET_LINK_LIBRARIES(${TARGET_NAME} m)
SET(CMAKE_C_FLAGS "-fopenmp -lpthread -lm -O3")